import { View, Text } from 'react-native';
import React from 'react';

const TeamsComponent = () => {
	return (
		<View>
			<Text>TeamsComponent</Text>
		</View>
	);
};

export default TeamsComponent;
