import { Box } from 'native-base';
import CreateTeamsContainer from '@features/dashboard/containers/CreateTeams.container';
const CreateTeamsScreen = () => (
	<Box safeArea flex="1" bg="#1D004C">
		<CreateTeamsContainer />
	</Box>
);

export default CreateTeamsScreen;
